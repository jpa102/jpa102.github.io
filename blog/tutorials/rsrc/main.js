/*
	internal config
*/
let vibratems = 80; // time to vibrate

// ==============================================================================

function vibrate() {
	navigator.vibrate(vibratems);
}
